let submitButton = document.querySelector("div.page label input.submit");
submitButton.addEventListener("click", () => {
  let userName = document.querySelector("div.page label input.userName").value;
  
});
